# Copy to .env and fill in values

# Ollama model to use for name parsing (must be pulled locally)
OLLAMA_MODEL=qwen3:8b

# Paths (defaults match the NAS mount points — override if needed)
# TORRENTS_DIR=/Volumes/Torrents/3TB Mirror/Torrents
# OUTPUT_DIR=/Volumes/video/Movies_AV1
